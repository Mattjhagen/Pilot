* LendingError: Enum capturing errors such as noOffersAvailable, applicationDenied(reason), paymentFailed, etc.
* LendingService: Protocol described in the Success Criteria.  Add additional methods as necessary.
* MockLendingService: Implementation using local JSON to produce offers and agreements.  Use simple rules to determine eligibility (e.g. trust score above a threshold, debt‑to‑income ratio not exceeding a percent).  Persist active loans and agreements in memory or local storage.  Simulate asynchronous calls with delays.
* LendingManager: Injects the selected LendingService (defaulting to MockLendingService).  Coordinates prequalification by requesting offers and caching them, processes applications, creates agreements on approval, and updates the Trust module when payments are made.  Provides published properties for offers, active loans and application status.  Provides methods to link payment accounts via the Money module when making repayments.

Trust Integration

* Use the Trust module to obtain the user’s Trust Score and metrics (income stability, savings habits, bill payment consistency, etc.).  Provide this data to the LendingService in prequalifyLoans to determine the offers and interest rates.  Ensure the integration is via a protocol to allow the service to consume trust data without directly depending on Trust module internals.

Money Integration

* On approval, credit the user’s selected linked account with the loan amount (mocked).  When a repayment is made, debit the selected account.  Use the Money module’s models and update flows to reflect these transactions.  The LendingManager should coordinate with the IntegrationManager from Phase 09 to select a payment account and record the transaction.
* Payment schedules should be generated by the lending service (mock) and stored as part of the LoanAgreement.  Each scheduled payment should appear in the Money module as an upcoming bill (via the existing bills system) and update to paid when executed.

UI & UX

* OffersView: Displays a list of LoanOffer cards.  Each card shows the principal, term, APR, and monthly payment.  Use friendly copy to explain the purpose of each offer.  If there are no offers, display an encouraging empty state explaining that increasing the Trust Score may unlock offers.
* OfferDetailView: Provides full details of a selected offer.  Show the breakdown of fees, interest, monthly payment schedule, and total repayment.  Provide a call‑to‑action button to proceed with the application.  Include educational tips about borrowing responsibly.
* ApplicationFormView: Collect any additional information required (e.g. verifying employment) in a series of sections.  Use our design system’s form components.  At the end, display a summary of the loan terms and require the user to agree to the terms and conditions (displayed as a modal or sheet).  Simulate an approval process with an animated progress indicator.  Show either success (with the new loan details) or rejection (with reasons and suggestions to improve the Trust Score).
* LoansListView: Shows a list of active LoanAgreement objects.  Each cell displays outstanding balance, next payment, and progress toward payoff.  Include a CTA to “Make Payment”.
* LoanDetailView: Shows detailed information about the selected loan: original principal, remaining balance, interest rate, due dates, past repayments (list of Repayment), and payment schedule (chart or list).  Provide actions to make a payment (select amount and payment account), view the agreement, or pay off early.  Use modals or sheets to handle payments.  Simulate error handling and success messages.

Use haptics and subtle animations to provide a sense of progression and success (e.g. when a loan is approved or a payment is made).  Follow the design system tokens for colors, typography, spacing and motion.  Ensure the flows feel premium and trustworthy.

Persistence

Persist loan agreements and repayment history using SwiftData or another persistent layer.  On app launch, rehydrate active loans into the LendingManager.  Ensure state restoration across app restarts.  Use unique identifiers for each loan and repayment record.

Testing

* Unit Tests: Validate the prequalifyLoans algorithm with different trust scores and income parameters.  Ensure the MockLendingService returns offers correctly and handles applications, payments and refresh logic.  Test LendingManager workflows, ensuring it correctly updates offers, agreements and payments.  Check that rejected applications provide appropriate reasons.
* UI Tests: Simulate user flows: viewing offers, applying for a loan, receiving approval, making payments, paying off early, and handling rejection or error states.  Verify that the UI updates accordingly and persistent data reflects changes.

Documentation

Create docs/architecture/Lending.md to describe:

* The purpose of the Lending module and how it integrates with Trust and Money.
* Data models and service protocol definitions.
* The prequalification algorithm (mock) and how trust metrics influence offers.
* Guidelines for implementing a real lending engine in the future (including regulatory and compliance considerations such as fair lending, KYC/AML, and data privacy).

Add an ADR entry in DECISIONS.md that covers the rationale for designing a trust‑based lending architecture, using a protocol to abstract the lending engine, and simulating offers and applications rather than implementing real credit functionality.

⸻

Design Requirements

* Transparency: Clearly explain loan terms, fees, interest rates, and total repayment.  Provide an “Educational” section with tips on responsible borrowing and how the Trust Score affects offers.
* Responsibility: Encourage users to borrow only what they need and warn them of potential consequences of late payments.  Avoid dark patterns; all CTAs should be explicit and clear.
* Inclusivity: Support users with varying trust scores by offering guidance on improving their score to qualify for better terms.  Provide accessible copy for people with different financial literacy levels.
* Consistency: Use the established design system from Phase 02.  Align with our navigation patterns, card components, typography and color palette.  Avoid introducing new design elements unless necessary.
* Accessibility: Ensure all forms and interactive elements are accessible via VoiceOver.  Provide descriptive labels and hints for controls.  Announce when loan status changes, payments succeed or fail, and when an application is approved or rejected.

⸻

Performance

* Defer heavy computations (e.g. generating payment schedules) to background threads.  Use structured concurrency and Observers to update the UI on the main thread.
* Cache offers and agreements to avoid repeated computation or loading.  Invalidate caches appropriately when the trust score or financial data changes.
* Consider memory management of large data sets (e.g. long repayment histories) in the future.

⸻

Accessibility

* Support Dynamic Type across all forms and views.
* Provide high contrast and adequate spacing for readability.
* Ensure error messages and status updates are vocalized by VoiceOver.
* Offer a way to view or download the loan agreement in an accessible format (e.g. as plain text or accessible PDF) using system share sheets.

⸻

Testing

Testing is essential to ensure fairness and stability in lending functionality.  Tests should cover:

* Eligibility computations relative to trust metrics.
* Correct display of offers based on mock service responses.
* Application approval and rejection flows with appropriate reasons.
* Payment processing flows and updates to loan and account balances.
* Persistence and restoration of loan agreements and repayment history.
* UI flows, including empty, loading, success and error states.

Use Swift Testing frameworks for unit tests and UI tests as you did in previous phases.  Mock dependencies where necessary.

⸻

Documentation & ADR

Write docs/architecture/Lending.md detailing the architecture, integration points, and algorithms used.  Add an entry in DECISIONS.md summarizing the choice to simulate lending, the use of a protocol for the lending service, and how trust metrics inform eligibility.

⸻

Completion

1. Implement and refine all components described above.  Ensure the UI flows from prequalification to loan management are cohesive and free of warnings or crashes.
2. Persist loan data and verify that the Money module reflects disbursements and repayments accurately.
3. Run all tests and ensure they pass.  Add new tests where coverage is lacking.
4. Document the architecture and reasoning.  Update DECISIONS.md with the lending ADR.
5. Do not proceed to any optional phases (e.g. Blockchain Anchoring) without explicit approval.  Summarize the completed work, highlight any challenges, and wait for feedback before continuing.
